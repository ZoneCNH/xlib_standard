# PLAN-GOAL-20260606-002-v0.1: docs/v0.6.0 Deep Analysis

Goal: GOAL-20260606-002
Task: TASK-GOAL-20260606-002-001
Date: 2026-06-06
Worktree: /home/xlib-standard/.worktree/workspaces/codex-v060-docs-analysis
Branch: codex/v060-docs-analysis
Final analysis HEAD: b6d27ab2e60d854e1e23faa4f1c2ffa79a35d421
Final analysis tree: fa64da03edb77e6510e3d22ae9f1edf0002115fc
Final analysis boundary: this revision identifies the reviewed deep-analysis output before later evidence/provenance cleanup commits on `codex/v060-docs-analysis`; it is not the current branch HEAD after omission-fix reconciliation.
Pre-addendum delivery snapshot: immediately before the latest final-reconciliation addendum, this branch was at `HEAD 9b33665e8fe735e69bd2a29ec61280696ef8e43a` with tree `62f22e172e2dd765e32ae374a4a70fbf2cb4f80b`; its parent `e9f06d8b3990e2072d1c33424bb3ddb48e922afa` is the accepted prior-runtime leader state recorded by `.omx/reports/team-commit-hygiene/fix-goal-20260606-002-131e4dc5.context.json`.
Worker-1 follow-up worktree: /home/xlib-standard/.worktree/workspaces/codex-v060-docs-analysis/.worktree/omx-team/fix-goal-20260606-002-131e4dc5/worker-1 (detached at final analysis HEAD)

## Scope

Analyze `/home/xlib-standard/docs/v0.6.0/` as read-only source material and produce an executable plan. This artifact does not edit or normalize the source docs.

The requested outcome is a ranked, evidence-aware plan for turning the v0.6.0 analysis set into governed implementation work, with explicit boundaries between source-document claims, repo-verified facts, and planning inference.

Delivery-state boundary: this plan is a branch artifact for `codex/v060-docs-analysis` and for local team-worker result aggregation into the analysis branch. It is not product/runtime integration, a `main`-branch delivery, release candidate, or release-readiness approval.

Integration wording boundary: "integration" in this plan means branch-local worker/leader artifact reconciliation unless a command explicitly names a merge/release operation. It does not mean merge to `main`, downstream adoption, or release readiness.

Self-reference boundary: statements about this plan, its evidence addenda, task transitions, or team-runtime commits describe the local analysis/reconciliation artifact itself. They are not independent proof that the plan's future P0/P1/P2 implementation work, release gates, downstream adoption, or `main` delivery have occurred.

## Input Set

The source directory contains 10 Markdown files and 14,028 lines. Three files are exact duplicate pairs by SHA:

- `strict-config-root-v4-closure-audit.md` equals `strict-config-root-v4-closure-audit (1).md`
- `strict-config-root-omission-audit.md` equals `strict-config-root-omission-audit (1).md`
- `xlib-standard-strict-config-root-goal-plan-v2-audited.md` equals `xlib-standard-strict-config-root-goal-plan-v2-audited (1).md`

Canonical lineage inferred from filenames, version markers, and worker SHA validation:

`xlib-standard-strict-config-root-goal-plan.md`
-> `xlib-standard-strict-config-root-goal-plan-v3-final-executable.md`
-> `strict-config-root-v4-closure-audit.md`
-> `xlib-standard-strict-config-root-goal-plan-v5-ultimate.md`

The v0.4.15 optimization document is a wider governance and proof-depth analysis that should constrain the strict-config-root work rather than replace the v5 closure plan.

Source-hygiene boundary: the `docs/v0.6.0/` source documents were observed as untracked files in `/home/xlib-standard` on `main`, while this analysis worktree does not contain that source directory. That state is a source-hygiene and provenance gap only; it is not implementation evidence, release evidence, or proof that `main` contains delivered docs.

Duplicate-evidence boundary: exact duplicate draft files are useful for SHA inventory and canonicalization decisions, but they do not count as independent corroborating evidence. Future citations should prefer the canonical source file and cite a duplicate only to document source hygiene.

## Evidence Boundary

Source-document claims:

- v5 defines the strict-config-root closure objective and P0 additions at `docs/v0.6.0/xlib-standard-strict-config-root-goal-plan-v5-ultimate.md:4` and `:45`.
- v4 identifies missing split between standard source and generated target at `docs/v0.6.0/strict-config-root-v4-closure-audit.md:14`.
- omission audit identifies platform-native gaps, remote policy gaps, release manifest gaps, rollback gaps, and negative-test gaps at `docs/v0.6.0/strict-config-root-omission-audit.md:14`, `:484`, `:553`, and `:641`.
- v0.4.15 defines proof-depth limits and release-blocking proof requirements at `docs/v0.6.0/xlib-standard-latest-v0.4.15-complete-optimization-20260605.md:449` and `:1198`.

Repo-verified facts:

- OMX team `deep-analyze-home-xli-131e4dc5` reached `phase=complete` with 3 of 3 tasks completed before shutdown.
- Initial team shutdown completed with `merge_outcome=noop` for all workers; no worker changes from that read-only deep-analysis team were merged into the leader branch.
- This analysis was performed in branch `codex/v060-docs-analysis`, not on `main`; worker worktrees are staging surfaces and do not by themselves prove main-branch or release delivery.
- Follow-up worker-1 changes referenced by the evidence artifact came from a detached OMX team worker worktree and were later reconciled into this branch; they are not direct edits to `/home/xlib-standard` main.

Delivery-state clarification:

- These artifacts are branch-local planning and evidence artifacts. They must not be read as proof that the analyzed source docs, team worker outputs, or strict-config-root implementation changes were delivered to `main`.
- Any transient or untracked docs observed in the main worktree are source-hygiene and artifact-placement gaps only. They are not accepted delivery evidence, implementation proof, or release-readiness evidence.
- Duplicate, copied, or draft source docs are inventory and canonicalization risks. They can identify lineage ambiguity, but they must not be counted as independent corroborating evidence.
- The D3 traceability boundary remains file-exists proof only; it cannot prove lifecycle graph closure, source-to-release closure, or downstream conformance.
- This plan makes no release-readiness claim. Release-final readiness remains conditional on clean workspace, release manifest, checksum chain, score, context-release evidence, final release gates, and downstream evidence where required.

Delivery-state reconciliation:

- The artifacts are branch-local analysis evidence for `codex/v060-docs-analysis`; worker shutdown outcomes, whether `merge_outcome=noop` or a local leader merge commit, are not evidence of delivery to `main` or a release branch.
- Transient or untracked files observed in the main worktree are source-hygiene and artifact-placement gaps only; they are not accepted delivery evidence for this goal.
- SHA-identical duplicate draft docs count once as source inventory and deduplication hygiene, not as independent corroborating evidence.
- D3/file-exists traceability can support file-presence checks only and must not be promoted to lifecycle closure or release-readiness proof.
- Latest omission-fix team reconciliation is recorded by `.omx/reports/team-commit-hygiene/fix-goal-20260606-002-131e4dc5.context.json`: worker-3 source `d3bba3b719d3eb625f602523f9da2c83091f6ee0` was attempted but its shutdown merge conflicted in both GOAL-20260606-002 evidence artifacts; worker-1 source `3310971c700e755a1495815457b1fd366699f2cb` was accepted into leader state `e9f06d8b3990e2072d1c33424bb3ddb48e922afa`; worker-2 was verification-only/no-op for source edits.
- Corrected task routing in the latest follow-up runtime treated worker-2 task 4 and worker-3 task 5 as report-only verification lanes. Any task-owner mismatch in tasks 2 or 3 is runtime orchestration evidence only, not source-delivery evidence.
- That reconciliation is branch-local evidence handling only. It does not prove main-branch delivery, downstream adoption, release readiness, or full lifecycle graph closure.
- Future finalization evidence must record predecessor/pre-addendum snapshots before committing, then verify any resulting commit hash/tree in a separate post-commit evidence event if needed. A commit cannot honestly claim that its own final hash/tree is already contained in the committed text.

Planning inference:

- The P0/P1/P2 ranking below is the integrated execution order implied by the source docs and worker reports. Exact issue IDs and schedule are not established by the source docs; owner roles for known out-of-scope gaps are listed below to make follow-up responsibility explicit without claiming those gaps are resolved.


## Coverage Matrix: v5 And v0.4.15 Claims To Plan Items

This matrix fixes the follow-up omission in the analysis artifact. It maps source-document claims to ranked plan work without treating those claims as implementation proof. Each row requires future implementation evidence before it can support release readiness.

| Source claim | Source evidence | Plan item(s) | Coverage status and required proof |
| --- | --- | --- | --- |
| v5 fact-domain split: `.config/xlib` owns standard facts; platform-native files are externally required entrypoints; legacy roots fail on presence. | `docs/v0.6.0/xlib-standard-strict-config-root-goal-plan-v5-ultimate.md:10-23`; `docs/v0.6.0/strict-config-root-v4-closure-audit.md:175-197` | P0-2, P0-4 | Covered as taxonomy and platform policy work. Required proof: typed path taxonomy schema, static graph evidence, and D5 negative fixtures for forbidden legacy roots and platform-native/xlib fact confusion. |
| v5 remote GitHub policy, CODEOWNERS, and enabled rulesets must be audited as release facts. | `xlib-standard-strict-config-root-goal-plan-v5-ultimate.md:29-33`, `:45-52`; `strict-config-root-omission-audit.md:507` | P0-4, P0-5 | Partially covered. Required proof: evidence record for branch protection, rulesets, Actions settings, effective CODEOWNERS, and `repository_rules_release_decision` in the release evidence chain. |
| v5 allowlist materialization, generated checksums, and full-copy render prevention. | `xlib-standard-strict-config-root-goal-plan-v5-ultimate.md:45-52`; `strict-config-root-omission-audit.md:395`, `:456` | P0-3, P0-5 | Covered. Required proof: allowlist-only render test, projection checksum, canonical encoder evidence, Docker-context evidence, and attestation/replay record. |
| v5 path bypass controls for submodule, symlink, worktree, case collision, path traversal, Docker, workflow, and fixture escapes. | `xlib-standard-strict-config-root-goal-plan-v5-ultimate.md:45-52`; `strict-config-root-v4-closure-audit.md:14-26`, `:607` | P0-4, P2-2 | Covered but not enumerated enough. Required proof: bypass-class negative fixture matrix with shell, Makefile, Go, `go:embed`, Dockerfile, workflow, submodule, worktree, casefold, and checksum-mismatch cases. |
| v5 `.config` secret scan and v0.4.15 security freshness are separate release-final facts. | `xlib-standard-strict-config-root-goal-plan-v5-ultimate.md:45-52`; `xlib-standard-latest-v0.4.15-complete-optimization-20260605.md:501-526` | P1-3, P0-5 | Partially covered. Required proof: max-age, scheduled-success, canonical `govulncheck` version, secret-scan inclusion, dependency/toolchain freshness, and canonical facts-version evidence. |
| v0.4.15 release facts must reconcile current version/tag, release projections, and drift-sensitive facts. | `xlib-standard-latest-v0.4.15-complete-optimization-20260605.md:3-5`, `:1195-1202` | P0-5 | Undercovered before this matrix. Required proof: objectized current-release fact set that reconciles tag, manifest, GoalCLI/version constants, changelog/release notes, source provenance, and release decision. |
| v0.4.15 proof depth D0-D7 governs release claims and downstream adoption. | `xlib-standard-latest-v0.4.15-complete-optimization-20260605.md:449-460`, `:1007-1061`, `:1195-1202` | P0-5, P2-2, P2-4 | Covered at high level. Required proof: every release-blocking gate records `proof_depth`; D4 minimum for release-blocking gates, D5 negative fixture before release-final, D6 replay/attestation where required, and D7 only for downstream conformance. |
| v0.4.15 evidence ledger/proof graph must be append-only or replayable; manifest is a projection, not the unique source. | `xlib-standard-latest-v0.4.15-complete-optimization-20260605.md:21-30`, `:135-139`, `:491-497` | P0-5 | Undercovered before this matrix. Required proof: append-only event ledger or replayable equivalent with hash chain and command, input, output, exit code, env, digest, time, runner, policy version, proof JSON, and manifest projection. |
| v0.4.15 metrics and scorecards need objectized contracts rather than prose-only confidence. | `xlib-standard-latest-v0.4.15-complete-optimization-20260605.md:82-91`, `:97-104` | P0-5, P2-2 | Gap identified. Required proof: metrics contract artifact with schema/digest/proof-depth plus scorecard fields for release-blocking gate count, D4+ gate ratio, release-blocking debt count, downstream D7 adoption count, and baseline score. |
| v0.4.15 task/debt objectization binds findings to invariant, location, evidence, severity, owner, expiry, remediation, and goal. | `xlib-standard-latest-v0.4.15-complete-optimization-20260605.md:135-139` | P0-5, P2-2 | Gap identified. Required proof: task matrix mapping P0/P1/P2 items to task IDs, invariants, acceptance criteria, source claims, gates, proof depth, evidence outputs, owner, expiry, and remediation path. |

## Required Object Model Addendum

This addendum makes the Required Object Model gap explicit. The rows below are future implementation requirements derived from the v5/v0.4.15 source docs; they are not proof that those objects already exist.

| Object family | Required future objectization |
| --- | --- |
| Release facts | Stable release fact object that reconciles current version/tag, release manifest, GoalCLI constants, changelog/release notes, source provenance, and release decision. |
| Proof depth | Gate evidence must carry `proof_depth`, with release-blocking claims separated from local-only or downstream-only proof. |
| Evidence ledger / proof graph | Append-only or replayable evidence-event object with command, inputs, outputs, exit code, env, digest, time, runner, policy version, proof JSON, and manifest projection. |
| Metrics / scorecard | Schema-backed metric object for gate count, D4+ gate ratio, release-blocking debt, downstream D7 adoption count, and baseline score. |
| Task / debt record | Findings mapped to task ID, invariant, location, evidence, severity, owner, expiry, remediation path, acceptance criteria, gate, and goal. |
| Downstream conformance | D7 adoption object that is backed by external downstream repository, CI, manifest, or reproducible smoke evidence. |
| Platform path taxonomy | Typed taxonomy for `platform-native`, `thin-adapter`, `generated-projection`, and `forbidden-legacy` roots. |
| Snapshot / profile / allowlist | Standard snapshot, config profile, allowlist, pathguard, checksum, and replay objects used by strict-config-root release gates. |

Traceability limitation: current `goalcli` token checking only existence-checks path-like tokens; descriptive tokens such as object-model names are not proof that lifecycle objects exist. Future implementation must therefore materialize the required objects as concrete files, schemas, contracts, or evidence events before claiming lifecycle graph closure.

## Known Proof Boundaries

- `traceability_status=partial_implemented`, `proof_depth=file_exists`, and `proof_depth_level=D3` can prove file-level existence only. They do not prove full lifecycle graph closure, implementation completeness, or release readiness.
- `make security` defaults to secret-scan-oriented evidence; dependency vulnerability freshness requires explicit weekly or forced `govulncheck` evidence before being claimed.
- Local contract/template checks do not prove downstream adoption. Downstream adoption needs external downstream repository, CI, manifest, or reproducible smoke evidence.
- Release-final readiness requires clean workspace, release manifest, checksum chain, score, context-release evidence, and final release gates together. Missing any one item remains a release gap; this plan must be read as an analysis roadmap, not a release-readiness assertion.

## Owners For Out-Of-Scope Gaps

| Gap | Owner for follow-up | Boundary |
| --- | --- | --- |
| Strict-config-root implementation changes | Strict-config-root implementation owner | This plan is an analysis roadmap, not implementation evidence. |
| Future object model/proof-depth/evidence ledger/metrics/task records | Release evidence/governance owner | Required before lifecycle or release-readiness claims. |
| D3/file-exists traceability boundary | Traceability/lifecycle owner | Must be upgraded before lifecycle closure claims. |
| Security freshness / scheduled or forced `govulncheck` | Security owner | Required before dependency freshness claims. |
| Missing `pathguard`, `platform-check`, and `github-policy-audit` gates | Harness/GoalCLI owner | Create and register before using as release gates. |
| Partially registered gates | Harness registry owner | Wire missing harness, command-registry, or target-registry entries. |
| Remote GitHub policy audit | Repository admin or release owner with `gh` credentials | External credentialed audit. |
| Downstream D7 adoption evidence | Downstream integration owner | Requires external downstream or reproducible smoke evidence. |
| Duplicate/untracked source docs and provenance hygiene | Documentation/provenance owner | Must be normalized before source-set freeze. |
| Main/release branch delivery and release readiness | Release/integration owner | Requires review/merge plus final release gates. |
| Full `CHECK_STATUS=passed GOWORK=off make evidence` gate | Release/evidence gate owner | Distinct from `evidence-check`; run before release-readiness claims. |

## Ranked Plan

### P0 - Release-Blocking Closure

P0-1. Freeze canonical source set and duplicate handling.

Acceptance criteria:
- Duplicates are marked non-canonical or removed in a later docs-normalization change.
- Future plans cite only canonical files unless a duplicate SHA is intentionally used as evidence.
- Evidence records SHA inventory and canonical lineage.

Required gates/evidence:
- `GOWORK=off make docs-check`
- `GOWORK=off make evidence-check`
- SHA inventory of `docs/v0.6.0/`

Rollback:
- Revert docs-normalization change only; source code behavior is unaffected.

P0-2. Define strict-config-root projection taxonomy.

Acceptance criteria:
- Every root path is classified as one of `platform-native`, `thin-adapter`, `generated-projection`, or `forbidden-legacy`.
- `forbidden-legacy` paths are hard failures.
- Platform-native entries such as GitHub rulesets and CODEOWNERS are not treated as ordinary copied files.

Required gates/evidence:
- New or updated strict config schema tests.
- `GOWORK=off make contracts`
- `GOWORK=off make boundary`
- `GOWORK=off make governance-check`, where available.

Rollback:
- Restore previous strict-config-root profile and keep the new taxonomy behind a disabled config flag until contracts pass.

P0-3. Replace copy-all-then-prune rendering with allowlist materialization.

Acceptance criteria:
- Generator renders only explicit allowlist material.
- Render verification fails on unexpected `.config` source leakage.
- Generated projections have stable checksum and attestation records.

Required gates/evidence:
- `GOWORK=off make render-check`
- `GOWORK=off make integration`
- Projection checksum evidence and downstream render smoke evidence.

Rollback:
- Stop rollout, retain previous generated target, and rerun render verification before retry.

P0-4. Harden PathGuard and platform policy checks.

Acceptance criteria:
- PathGuard blocks symlink, submodule, worktree, case-collision, path traversal, `go:embed`, fixtures, Docker context, and workflow bypasses described in v4/v5.
- Remote GitHub policy audit covers branch protection, rulesets, Actions settings, and CODEOWNERS effective state.
- Local `.github/rulesets/*.json` drift from remote enabled state is release-blocking.

Required gates/evidence:
- `GOWORK=off make pathguard`
- `GOWORK=off make platform-check`
- `GOWORK=off make github-policy-audit`
- Negative-test matrix for every bypass class.

Rollback:
- Fail closed: block release and keep previous manifest/checksum chain.

P0-5. Promote proof-depth and release evidence to typed contracts.

Acceptance criteria:
- Release-blocking claims require typed `proof_depth`; D4 is the minimum for release-blocking gates, and D5 negative-fixture proof is required before release-final promotion.
- Downstream adoption is only claimed with D7 downstream conformance evidence.
- Current release facts are objectized and reconcile tag, manifest, GoalCLI/version constants, changelog/release notes, source provenance, and release decision.
- Evidence ledger is append-only or replayable, includes hash-chain or equivalent integrity evidence, and records command, input, output, exit code, env, digest, time, runner, and policy version.
- Release manifest is a projection of the evidence ledger and records remote policy decision, projection checksums, config profile, and evidence chain.
- Metrics contract changes are objectized as contract artifacts with schema, digest, and proof depth rather than inferred from prose-only docs.
- Task/debt records bind invariant, location, evidence, severity, owner, expiry, remediation, goal, gate, proof depth, and evidence output.

Required gates/evidence:
- `GOWORK=off make release-check`
- `GOWORK=off make release-final-check`
- `goalcli score` where applicable.

Rollback:
- Do not publish release; retain prior release manifest and record a release gap.

### P1 - Standard Kernel Feasibility

P1-1. Split GoalCLI governance surface from implementation-heavy internals.

Acceptance criteria:
- Governance commands map to command registry entries.
- Changes to command behavior update CLI contracts, Makefile entries, and tests.

P1-2. Wire strict profile policy through constants, fixtures, env overrides, and migration docs.

Acceptance criteria:
- Effective profile handling is test-covered.
- Environment overrides cannot silently weaken strict behavior.

P1-3. Add security freshness and `.config` secret-scan inclusion.

Acceptance criteria:
- `.config` is included in secret scanning.
- Vulnerability-scan claims distinguish secret scan evidence from `govulncheck` evidence.
- Security freshness has max-age, scheduled-success, canonical `govulncheck` version, dependency/toolchain freshness, and canonical facts-version evidence.

P1-4. Define downstream goalcli distribution and pinned invocation policy.

Acceptance criteria:
- Downstream execution is pinned and reproducible.
- Local contract proof is not described as proof-based downstream adoption.

### P2 - Operationalization And Cleanup

P2-1. Publish a strict cutover and rollback runbook.

P2-2. Complete negative tests and release confidence matrix.

Acceptance criteria:
- Negative fixture matrix records gate id, invariant, source fixture, bad fixture, expected failure, proof depth, owner, and regression command.
- Bypass coverage includes symlink, submodule, worktree, casefold, path traversal, `go:embed`, fixture escape, Dockerfile `COPY`, workflow SHA pinning, score-threshold bypass, checksum mismatch, downstream full-config copy, and release-check without remote audit.
- Release confidence matrix records whether each gate is existence-only, static graph, execution, negative fixture, replay/attestation, or downstream conformance.
- Metrics/scorecard records release-blocking gate count, D4+ gate ratio, release-blocking debt count, downstream D7 adoption count, and baseline score.


P2-3. Normalize duplicate docs and create a concise canonical index for v0.6.0.

P2-4. Add downstream checksum, attestation, and adoption evidence once an external downstream repository or reproducible smoke target exists.

Acceptance criteria:
- No downstream adoption claim is made below D7 downstream conformance.
- External downstream or reproducible smoke evidence records checksum, attestation, pinned goalcli invocation, effective profile, and accepted evidence path.


## Recommended Gate Sequence

Evidence-gate distinction: `GOWORK=off make evidence-check` validates the registry contract (`Makefile:286-287`; harness ID `evidence_check` at `.agent/harness/harness.yaml:135-136`). The full evidence-generation gate is `CHECK_STATUS=passed GOWORK=off make evidence` (`Makefile:226`; harness command at `.agent/harness/harness.yaml:192`; release-required gate at `.agent/release/release-required-gates.yaml:124`) and must be run before release-readiness claims.

1. `GOWORK=off make docs-check`
2. `GOWORK=off make rules-verify`
3. `GOWORK=off make evidence-check`
4. `GOWORK=off make contracts`
5. `GOWORK=off make boundary`
6. `GOWORK=off make pathguard`
7. `GOWORK=off make platform-check`
8. `GOWORK=off make github-policy-audit`
9. `GOWORK=off make render-check`
10. `GOWORK=off make integration`
11. `GOWORK=off make release-check`
12. `GOWORK=off make release-final-check`

Evidence gate boundary: `GOWORK=off make evidence-check` is a validation check
for repository evidence/registry consistency. Full release evidence generation is
the distinct `CHECK_STATUS=passed GOWORK=off make evidence` gate and must be
recorded separately when it is actually run.

## Harness Gate Existence Matrix

This matrix checks the recommended gates against the current repository harness
surfaces: `Makefile`, `.agent/harness/harness.yaml`,
`.agent/registries/command-registry.yaml`, and
`.agent/registries/makefile-target-registry.yaml`. `harness.yaml` is split into
declared gate IDs and `make <target>` commands because some gates are invoked
inside suites without having their own harness ID.

| Gate | Makefile target | harness ID | harness command | command registry | target registry | Status |
| --- | --- | --- | --- | --- | --- | --- |
| `docs-check` | yes | no | yes | yes | yes | Runnable through suites; no standalone harness ID. |
| `rules-verify` | yes | no | no | yes | yes | Registered target; missing harness suite invocation. |
| `evidence-check` | yes | yes (`evidence_check`) | yes | yes | yes | Runnable as harness ID `evidence_check`; this is the registry contract check, not full evidence generation. |
| `CHECK_STATUS=passed GOWORK=off make evidence` | yes (`evidence`) | no | yes | no | yes (`evidence`) | Full evidence-generation gate used by harness/release flow; distinct from `evidence-check`. |
| `contracts` | yes | yes | yes | yes | yes | Fully represented. |
| `boundary` | yes | yes | yes | yes | yes | Fully represented. |
| `pathguard` | no | no | no | no | no | Harness gap; gate must be created and registered before use. |
| `platform-check` | no | no | no | no | no | Harness gap; gate must be created and registered before use. |
| `github-policy-audit` | no | no | no | no | no | Harness gap; also requires credential/rate-limit handling. |
| `render-check` | no | no | no | yes | no | Command registry exists, but Makefile/target registry/harness wiring is absent. |
| `integration` | yes | yes | yes | yes | no | Runnable and command-registered; target registry entry is absent. |
| `release-check` | yes | no | yes | no | yes | Runnable through suites; missing command registry and standalone harness ID. |
| `release-final-check` | yes | no | yes | yes | yes | Runnable through suites; no standalone harness ID. |

Matrix conclusions:

- Existing documentation/evidence and core local validation gates are mostly
  wired, but not all are exposed as standalone harness IDs.
- `pathguard`, `platform-check`, and `github-policy-audit` do not currently
  exist on any checked harness surface and must be implemented before the plan
  can require them as executable gates.
- Partial rows are harness gaps, not passes: future completion evidence should
  either wire the missing surfaces or explicitly record why a narrower surface
  is sufficient.

Gate-owner boundary:

- `pathguard` and `platform-check`: implementation/platform owner.
- `github-policy-audit`: release/platform governance owner with credential and rate-limit handling.
- `render-check`, `integration` target-registry wiring, and standalone harness IDs: harness/release tooling owner.
- Full `CHECK_STATUS=passed GOWORK=off make evidence` and release-final manifest/checksum proof: evidence/release owner.

## Risks

- Remote GitHub policy audit may require credentials and can be blocked by rate limits or missing repository permissions; future owner: release/platform governance owner.
- Some required gates named by v5 may not exist yet; absence should be recorded as a harness gap, not silently treated as pass; future owner: harness/release tooling owner.
- Downstream adoption cannot be claimed until external downstream evidence exists; future owner: downstream integration owner.
- Duplicate docs can create false confidence if treated as independent corroboration; future owner: docs/source-hygiene owner.
- Security freshness cannot be claimed without scheduled or forced `govulncheck` evidence; future owner: security owner.
- Full release evidence generation and release-final proof remain separate from `evidence-check`; future owner: evidence/release owner.

## Stop Condition

This analysis task is complete when the OMX team outputs are reconciled, the plan and evidence are written, applicable local documentation gates are run, and any skipped or failed checks are recorded without overstating completion.
