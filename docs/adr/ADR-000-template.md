# ADR-000：模板决策记录

> 模板说明：本文件是 ADR 写作模板，不代表已经批准的架构决策；`待定` 和
> `{{MODULE_NAME}}` 占位符必须在具体 ADR 中替换后才可作为证据引用。

## 状态

待定

## 背景

描述 `{{MODULE_NAME}}` 的决策背景。

## 决策

描述已选择的方案和被拒绝的替代方案。

## 影响

记录权衡、迁移影响和后续工作。

## Evidence

链接到测试、Harness、manifest 和评审记录。
