# 计划

## 里程碑

1. 统一 `xlib-standard` 五类职责和旧名迁移口径。
2. 补齐 ADR、下游矩阵、x.go 集成边界和 secret policy。
3. 补齐 `.agent` Full Goal Runtime v3.1 对象模型、状态机、traceability、Harness、Evidence、review、release、retrospective 和 patch 文件。
4. 由并行工作流完成 module/name 迁移、core gate CLI、manifest/security/score 实现和 kernel downstream smoke。
5. 运行文档、Go、contract、boundary、release 和 score gate，收集 `DONE with evidence:`。
