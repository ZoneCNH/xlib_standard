# Debt Register

The active debt register is generated from `.agent/policies/debt/*.yaml` policy files and `.agent/registries/debt/*.yaml` registries and the `goalcli debt` scanner.
Release artifacts are generated under `release/debt/` and are intentionally ignored.
