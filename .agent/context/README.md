# Agent Context

本目录保留 Agent 上下文恢复材料，例如运行快照、handoff、目标摘要或后续要沉淀的 context pack。

本目录只提供事实恢复和交接，不覆盖 `CONSTITUTION.md`、`.agent/rules/`、`.agent/harness/`、`contracts/` 或 `docs/architecture/`。

当前目录至少保留本 README，避免 Agent 在启动顺序中遇到空目录或缺失路径。没有专用 context pack 时，按 `.agent/INDEX.md` 的恢复顺序读取上游文件。
