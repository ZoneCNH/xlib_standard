# goal-patch.md 落地 audit 报告

**生成时间**: 2026-06-03  
**裁决依据**: goal-patch.md v1.0–v2.2 中的 8 条铁律  
**裁决方法**: 三问（真实痛点 / 是否已有等价实现 / 是否 ≤1PR 可落地）

---

## 一、八条铁律 vs 当前仓库覆盖矩阵

| # | 铁律 | 等价实现 | 状态 | 真实缺口 |
|---|---|---|---|---|
| 1 | RULE-CORE-001 没有 Evidence 不允许 DONE | `.agent/evidence/`、`goalcli evidence-check`、`release/evidence/` | ✅ 完备 | 无 |
| 2 | RULE-CORE-002 必须从真实上下文开始 | `.agent/context/` 包系统、`goalcli context-profile*` | ✅ 完备 | 无 |
| 3 | RULE-CORE-003 需求必须可验证 | `.agent/acceptance-matrix.yaml`、`goalcli acceptance-matrix` | ✅ 已有 | 无 |
| 4 | RULE-CORE-004 所有变更必须可追踪 | `.agent/traceability-matrix.yaml` | ✅ 已有 | 无 |
| 5 | RULE-CORE-005 Harness 是机器裁判 | `cmd/goalcli/`（82 Makefile target / ~90 子命令） | ✅ 完备 | 无 |
| 6 | RULE-CORE-006 Self-improving 强制 | `.agent/retrospective-*`、`goalcli retro-*` | ⚠️ 部分 | 无强制 Retro Gate |
| 7 | RULE-WORKTREE-001 禁止 main 开发 | `.githooks/`、`scripts/harness/no-main-dev.sh`、`.github/workflows/worktree-guard.yml` | 🔴 **未生效** | **`core.hooksPath` 未指向 `.githooks`，hooks 形同虚设** |
| 8 | RULE-SECRET-001 secret-check 是 P0 Gate | `scripts/check_secrets.sh`、`make security` | ⚠️ 部分 | secret-check 不在 pre-commit 路径 |

---

## 二、关键发现（按严重度）

### 🔴 P0-1：本地 hooks 未启用（最大真实缺口）
```bash
$ git config --get core.hooksPath
# 空输出 → Git 默认使用 .git/hooks/，那里是空目录
$ ls .git/hooks/ | grep -v sample
# 空
```
**后果**：`.githooks/pre-commit` 和 `pre-push` 两个文件存在但永不执行。  
RULE-WORKTREE-001 在本地完全靠"开发者自觉"，与文档声称的"机器铁律"严重背离。  
**修复成本**：1 行命令 + 1 行 Makefile target。

### 🔴 P0-2：secret-check 未挂入 pre-commit
`.githooks/pre-commit` 只检查分支名，未调用 `scripts/check_secrets.sh`。  
**后果**：密钥可以在本地 commit 通过，仅在 CI（security.yml）拦截，违反"快速反馈"原则。  
**修复成本**：pre-commit 增加 ~3 行。

### 🟡 P1-1：worktree-guard CI 只在 PR 时触发
`.github/workflows/worktree-guard.yml` 只挂 `pull_request`，**push 到 main 不会触发**。
配合"hooks 未启用 + main 无 branch protection"（待确认），意味着 main 仍可被直接 push。
**修复成本**：workflow 增加 `push: branches: [main]` 触发 + 检查 `github.event.pusher`，5 行。

### 🟢 P2-1：goalcli 单一命名
goal-patch.md v2.0+ 要求机器裁判与执行器使用 `goalcli`。
**裁决更新**：文档、代码、schema、Evidence 和 Makefile 统一改名为 `goalcli`，不再保留历史旧名的并列权威。

### 🟢 P2-2：`.agent/` 90 文件平铺，未按 v1.8 九层分目录
当前结构：所有 yaml/md 平铺在 `.agent/` 根（仅 `rules/`、`evidence/`、`debt/` 等少数子目录）。  
v1.8 建议拆为 L0–L9 九层。  
**裁决**：**不重构**。物理迁移成本 > 收益，且会击穿所有 PR/Issue 引用链。改用"文档分类标签"代替物理迁移。

---

## 三、文档全量 vs 真实需要的收敛比

| 维度 | goal-patch.md 全量 | 真实需要 | 收敛比 |
|---|---|---|---|
| 规则条数 | 300+ | 8（铁律） + ~15（已实现） | ~95% 不需新做 |
| 必须新增的目录 | `.agent/{schemas,policies,registries,goals,constitution}/` 等 9 个 | 0（沿用现有结构） | 100% 砍掉 |
| 必须新增的命令 | goalcli 全套 ~30 个 | 0（现有 `cmd/goalcli` 已覆盖） | 100% 砍掉 |
| 必须新增的文档 | 海量 | 3 个：归档原文 + canonical + 单一命名合约 | 极小 |
| 必须新增的代码 | 第二套 goalcli Go 内核 | 0 行（仅 hook 启用 + secret 挂入 pre-commit） | ~99% 砍掉 |

---

## 四、最小落地清单（按优先级）

| 优先级 | 动作 | 成本 | 价值 |
|---|---|---|---|
| **P0-1** | 启用 `.githooks` 并把 `git config core.hooksPath .githooks` 纳入 `make doctor` / bootstrap | 5 分钟 | 直接消除最大铁律漏洞 |
| **P0-2** | `.githooks/pre-commit` 增加调用 `scripts/check_secrets.sh` | 5 分钟 | secret 提前一步拦截 |
| **P1-1** | `worktree-guard.yml` 增加 push 触发 | 5 分钟 | 闭环 CI 防护 |
| **P0-3** | 文档归档：把 goal-patch.md 复制到 `.agent/inbox/goal-patch-v1.0-to-v2.2.md` 作为只读原始记录 | 5 分钟 | 让规格成为版本化资产 |
| **P0-4** | 写 `.agent/standard/goal-runtime-canonical.md`（≤800 行，仅 8 铁律 + 9 层架构 + v0.1.0 五主线） | 1 小时 | 用 canonical 替代 13856 行原文 |
| **P0-5** | 写 `.agent/standard/goalcli-mapping.md`（单一命名合约） | 30 分钟 | 解决命名冲突 |
| **P2** | 不做：`.agent/` 物理重构、goalcli 内核新建、Trust Score / Rule Compiler 等 | 0（明确拒绝） | 防止过度工程 |

**总投入估算**：~3 小时（一次 PR 可完成）。  
**对比文档全量实现估算**：~30 天。  
**收敛比**：~99%。

---

## 五、结论

> **goal-patch.md 的真实价值是 8 条铁律 + 9 层架构思想，其余 95%+ 内容是"如果资源无限我们想做什么"。**
> 当前仓库已用 goalcli + .agent + .githooks + GHA 实现了 7/8 铁律的大部分。
> 真正的剩余工作是**修一个被遗忘的 `git config`**，然后归档+裁决文档本身。

下一步进入 Layer 2/3：产出 canonical 标准草稿 + 落地脚本。


---

> 本文件被 `scripts/check_docs.sh` 列为 required，删除会阻断 `make docs-check` / `make governance-check` / release pipeline。
