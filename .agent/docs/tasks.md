# 任务

- TASK-XLS-001：统一 README、docs/standard、ADR 与 `.agent` 的 `xlib-standard` 主身份。
- TASK-XLS-002：记录旧 `baselib-template` / `foundationx` 到 `xlib-standard` / `kernel` 的迁移规则。
- TASK-XLS-003：补齐 `docs/downstream-matrix.md` 与 `docs/xgo-integration-boundary.md`。
- TASK-XLS-004：声明 `/home/k8s/secrets/env/*` 仅为调用方路径，禁止进入源码、日志、manifest、PR 和 Evidence。
- TASK-XLS-005：补齐 `.agent` Full Goal Runtime v3.1 runtime 文件。
- TASK-XLS-006：完成 module path、imports、integration 默认名和 generator 渲染迁移。
- TASK-XLS-007：实现/校验 `goalcli`、release manifest、security 和 score gate。
- TASK-XLS-008：运行最终验证并以 `DONE with evidence:` 记录。
