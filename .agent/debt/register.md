# Debt Register

The active debt register is generated from `.agent/debt/*.yaml` policy files and the `goalcli debt` scanner.
Release artifacts are generated under `release/debt/` and are intentionally ignored.
