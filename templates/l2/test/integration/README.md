# L2 Integration Tests

Use this directory for provider-backed integration scenarios. The L2 template keeps provider connections and Contract Runner execution downstream; adopting repositories wire these tests with testkitx and xlibgate.
