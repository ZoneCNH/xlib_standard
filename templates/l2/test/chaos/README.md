# L2 Chaos Tests

Use this directory for failure injection and resilience scenarios. The L2 template keeps provider connections and Contract Runner execution downstream; adopting repositories wire these tests with testkitx and xlibgate.
