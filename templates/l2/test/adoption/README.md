# L2 Adoption Tests

Use this directory for downstream adoption and release-readiness checks. The L2 template keeps provider connections and Contract Runner execution downstream; adopting repositories wire these tests with testkitx and xlibgate.
