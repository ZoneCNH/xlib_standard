# L2 Adoption Tests

Use this directory for downstream adoption and release-readiness checks. xlib-standard does not provide provider connections or Contract Runner execution; downstream L2 repositories wire these tests with testkitx and xlibgate.
